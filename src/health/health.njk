---
pagination:
  data: collections.health
  size: 9
  alias: posts
---

{% extends 'layouts/default.njk' %}
{% from "partials/card.njk" import card %}

{% set title = "Health" %}

{% block content %}
    <div class="row">
        {% for post in collections.health %}
            {{ card(post) }}
        {% endfor %}
    </div>
{% endblock%}
